import type { AugustLockStatus } from '../types.js';
/**
 * * Lock or unlock a lock
 *
 * @param {string} action (used internally)
 * @param {string} [lockId]
 * @return {AugustLockStatus}
 */
export default function lockUnlock(this: any, action: string, lockId: string): Promise<AugustLockStatus>;
